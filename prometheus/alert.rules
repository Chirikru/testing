groups:
- name: error code
  rules:

  - alert: service_down
    expr: probe_http_status_code >= 500
    for: 0m
    labels:
      severity: Critical
    annotations:
      summary: "site down"
      description: "AHTUNG Site DOWN"

